<views:MvxWpfView x:Class="Storix.Presentation.Components.DashboardView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:loadingSpinnerControl="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:resources="clr-namespace:Storix.Presentation.Resources"
                  xmlns:viewModels="clr-namespace:Storix.Core.ViewModels;assembly=Storix.Core"
                  xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
                  d:DataContext="{d:DesignInstance Type=viewModels:DashboardViewModel,
                                                   IsDesignTimeCreatable=True}"
                  d:DesignHeight="auto"
                  d:DesignWidth="auto"
                  mc:Ignorable="d">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.Resources>
                <BooleanToVisibilityConverter x:Key="ToVisibilityConverter" />
            </Grid.Resources>
            <StackPanel VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource ToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                <loadingSpinnerControl:LoadingSpinner Diameter="100"
                                                      IsLoading="{Binding IsLoading}"
                                                      Thickness="3" />

                <TextBlock Margin="0,10"
                           FontFamily="Amita"
                           Foreground="{DynamicResource PrimaryTextColor}"
                           Text="Loading analytics data..."
                           TextAlignment="Center" />
            </StackPanel>

            <Grid Margin="20" Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Header Section  -->
                <StackPanel Grid.Row="0" Margin="0,0,0,25">
                    <TextBlock Margin="0,0,0,8"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="{DynamicResource PrimaryHeaderColor}"
                               Text="Analytics Dashboard" />
                    <TextBlock FontSize="16"
                               Foreground="{DynamicResource SecondaryHeaderColor}"
                               Text="Comprehensive insights into your inventory performance" />
                </StackPanel>

                <!--  Key Metrics Cards  -->
                <WrapPanel Grid.Row="1">
                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/revenue.png"
                                           CardTitle="Total Revenue"
                                           CardValue="{Binding TotalRevenue}"
                                           ChangePercentage="{Binding RevenueChange}"
                                           IconBackground="#D1FAE5" />

                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/products.png"
                                           CardTitle="Total Products"
                                           CardValue="{Binding TotalProductsDisplay}"
                                           ChangePercentage="{Binding ProductsChange}"
                                           IconBackground="#DBEAFE" />

                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/orders.png"
                                           CardTitle="Active Orders"
                                           CardValue="{Binding ActiveOrdersDisplay}"
                                           ChangePercentage="{Binding OrdersChange}"
                                           IconBackground="#FEF3C7" />

                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/alert.png"
                                           CardTitle="Low Stock Items"
                                           CardValue="{Binding LowStockItemsDisplay}"
                                           ChangePercentage="Action needed"
                                           IconBackground="#FEE2E2" />

                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/profit.png"
                                           CardTitle="Profit Margin"
                                           CardValue="{Binding ProfitMargin}"
                                           ChangePercentage="{Binding ProfitMarginChange}"
                                           IconBackground="#E9D5FF" />

                    <resources:CustomCards Background="{DynamicResource CardBackgroundColor}"
                                           BorderBrush="{DynamicResource CardBorderColor}"
                                           CardImage="/Assets/Icons/turnover.png"
                                           CardTitle="Inventory Turnover"
                                           CardValue="{Binding InventoryTurnover}"
                                           ChangePercentage="{Binding TurnoverChange}"
                                           IconBackground="#CFFAFE" />
                </WrapPanel>

                <!--  Charts Section  -->
                <Grid Grid.Row="2" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>

                    <!--  Revenue Trend Chart  -->
                    <Border Grid.Column="0"
                            Padding="20"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource CardBorderColor}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <StackPanel>
                            <Grid Margin="0,0,0,20">
                                <TextBlock HorizontalAlignment="Left"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryHeaderColor}"
                                           Text="Revenue Trend" />
                                <ComboBox Width="120"
                                          HorizontalAlignment="Right"
                                          SelectedIndex="{Binding SelectedTimePeriodIndex, Mode=TwoWay}">
                                    <ComboBoxItem Content="Last 7 days" />
                                    <ComboBoxItem Content="Last 30 days" />
                                    <ComboBoxItem Content="Last 3 months" />
                                    <ComboBoxItem Content="Last year" />
                                </ComboBox>
                            </Grid>
                            <!--  <lvc:CartesianChart Height="280"  -->
                            <!--  d:DataContext="{d:DesignInstance Type=viewModels:DashboardViewModel}"  -->
                            <!--  AnimationsSpeed="00:00:00.5"  -->
                            <!--  Series="{Binding RevenueChartSeries}"  -->
                            <!--  TooltipPosition="Top"  -->
                            <!--  XAxes="{Binding RevenueChartXAxes}"  -->
                            <!--  YAxes="{Binding RevenueChartYAxes}" />  -->
                        </StackPanel>
                    </Border>

                    <!--  Top Products  -->
                    <Border Grid.Column="2"
                            Padding="20"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource CardBorderColor}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,15"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryHeaderColor}"
                                       Text="Top Products" />

                            <!--  ItemsControl bound to TopProducts collection  -->
                            <ItemsControl ItemsSource="{Binding TopProducts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <resources:TopProductItem ProductName="{Binding ProductName}"
                                                                  Rank="{Binding Rank}"
                                                                  Revenue="{Binding TotalRevenue}"
                                                                  UnitsSold="{Binding UnitsSold}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <!--  Additional Analytics Section  -->
                <Grid Grid.Row="3" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Sales by Category  -->
                    <Border Grid.Column="0"
                            Padding="20"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource CardBorderColor}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,20"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryHeaderColor}"
                                       Text="Sales by Category" />

                            <Border Height="220"
                                    Background="{DynamicResource ChartPlaceholderBackground}"
                                    BorderBrush="{DynamicResource CardBorderColor}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="16"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="🥧 Pie Chart" />
                            </Border>
                        </StackPanel>
                    </Border>

                    <!--  Stock Levels  -->
                    <Border Grid.Column="2"
                            Padding="20"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource CardBorderColor}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,20"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryHeaderColor}"
                                       Text="Stock Levels" />

                            <Border Height="220"
                                    Background="{DynamicResource ChartPlaceholderBackground}"
                                    BorderBrush="{DynamicResource CardBorderColor}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="16"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="📊 Bar Chart" />
                            </Border>
                        </StackPanel>
                    </Border>

                    <!--  Order Status  -->
                    <Border Grid.Column="4"
                            Padding="20"
                            Background="{DynamicResource CardBackgroundColor}"
                            BorderBrush="{DynamicResource CardBorderColor}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,20"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryHeaderColor}"
                                       Text="Order Status" />

                            <StackPanel>
                                <Border Margin="0,0,0,10"
                                        Padding="15"
                                        Background="#FFFBEB "
                                        CornerRadius="8">
                                    <Grid>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1E293B"
                                                   Text="Draft" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="#F59E0B"
                                                   Text="{Binding DraftOrders}" />
                                    </Grid>
                                </Border>

                                <Border Margin="0,0,0,10"
                                        Padding="15"
                                        Background="#DBEAFE"
                                        CornerRadius="8">
                                    <Grid>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1E293B"
                                                   Text="Active" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="#3B82F6"
                                                   Text="{Binding ActiveOrders}" />
                                    </Grid>
                                </Border>
                                <Border Margin="0,0,0,10"
                                        Padding="15"
                                        Background="#F3E8FF "
                                        CornerRadius="8">
                                    <Grid>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1E293B"
                                                   Text="Fulfilled" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="Violet"
                                                   Text="{Binding FulfilledOrders}" />
                                    </Grid>
                                </Border>

                                <Border Margin="0,0,0,10"
                                        Padding="15"
                                        Background="#D1FAE5 "
                                        CornerRadius="8">
                                    <Grid>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1E293B"
                                                   Text="Completed" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="#10B981"
                                                   Text="{Binding CompletedOrders}" />
                                    </Grid>
                                </Border>

                                <Border Padding="15"
                                        Background="#FEE2E2 "
                                        CornerRadius="8">
                                    <Grid>
                                        <TextBlock FontWeight="SemiBold"
                                                   Foreground="#1E293B"
                                                   Text="Cancelled" />
                                        <TextBlock HorizontalAlignment="Right"
                                                   FontSize="20"
                                                   FontWeight="Bold"
                                                   Foreground="#EF4444"
                                                   Text="{Binding CancelledOrders}" />
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!--  Performance Indicators  -->
                <Border Grid.Row="4"
                        Padding="20"
                        Background="{DynamicResource CardBackgroundColor}"
                        BorderBrush="{DynamicResource CardBorderColor}"
                        BorderThickness="1"
                        CornerRadius="12">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,20"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryHeaderColor}"
                                   Text="Key Performance Indicators" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!--  Average Order Value  -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <TextBlock Margin="0,0,0,8"
                                           FontSize="14"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="Avg. Order Value" />
                                <TextBlock Margin="0,0,0,5"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           Text="$558" />
                                <Border Padding="8,4"
                                        HorizontalAlignment="Left"
                                        Background="#D1FAE5"
                                        CornerRadius="12">
                                    <TextBlock FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="#065F46"
                                               Text="↑ 7.2%" />
                                </Border>
                            </StackPanel>

                            <!--  Conversion Rate  -->
                            <StackPanel Grid.Column="1" Margin="0,0,15,0">
                                <TextBlock Margin="0,0,0,8"
                                           FontSize="14"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="Conversion Rate" />
                                <TextBlock Margin="0,0,0,5"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           Text="68.3%" />
                                <Border Padding="8,4"
                                        HorizontalAlignment="Left"
                                        Background="#D1FAE5"
                                        CornerRadius="12">
                                    <TextBlock FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="#065F46"
                                               Text="↑ 2.1%" />
                                </Border>
                            </StackPanel>

                            <!--  Customer Retention  -->
                            <StackPanel Grid.Column="2" Margin="0,0,15,0">
                                <TextBlock Margin="0,0,0,8"
                                           FontSize="14"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="Customer Retention" />
                                <TextBlock Margin="0,0,0,5"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           Text="84.7%" />
                                <Border Padding="8,4"
                                        HorizontalAlignment="Left"
                                        Background="#D1FAE5"
                                        CornerRadius="12">
                                    <TextBlock FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="#065F46"
                                               Text="↑ 5.3%" />
                                </Border>
                            </StackPanel>

                            <!--  Days to Fulfill  -->
                            <StackPanel Grid.Column="3">
                                <TextBlock Margin="0,0,0,8"
                                           FontSize="14"
                                           Foreground="{DynamicResource SecondaryTextColor}"
                                           Text="Avg. Days to Fulfill" />
                                <TextBlock Margin="0,0,0,5"
                                           FontSize="28"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           Text="2.4 days" />
                                <Border Padding="8,4"
                                        HorizontalAlignment="Left"
                                        Background="#D1FAE5"
                                        CornerRadius="12">
                                    <TextBlock FontSize="11"
                                               FontWeight="SemiBold"
                                               Foreground="#065F46"
                                               Text="↓ 0.3 days" />
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

            </Grid>

        </Grid>
    </ScrollViewer>
</views:MvxWpfView>